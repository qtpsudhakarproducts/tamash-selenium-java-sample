# Browser tests share a machine and a live app — keep them sequential.
junit.jupiter.execution.parallel.enabled=false

# Cucumber: publish nothing to the cloud, run features single-threaded.
cucumber.execution.parallel.enabled=false
cucumber.publish.quiet=true
